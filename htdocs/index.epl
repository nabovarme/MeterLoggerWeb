<HTML>
	<HEAD>
		<TITLE>MeterLogger</TITLE>
		<script src="dygraphs/dygraph-dev.js"></script>
		<style type="text/css">
		.default {
			font-family: Verdana, Geneva, sans-serif;
		}
		</style>
	</HEAD>
	<BODY>
[- 
use DBI;

#use lib qw( /var/www/lib/perl );
use lib qw( /opt/local/apache2/perl );
use Nabovarme::Db;

if ($dbh = Nabovarme::Db->my_connect) {
	my $quoted_serial = $dbh->quote($serial);
	$sth = $dbh->prepare(qq[SELECT DISTINCT(`serial`) from samples ORDER BY `serial` ASC]);
	$sth->execute;
}
-]
<span class="default">Meters</span><br>
[$ if ($sth->rows) $]
[$ while ($d = $sth->fetchrow_hashref) $]
		<span class="default"><a href="detail.epl?serial=[+ $d->{serial} +]">[+ $d->{serial} +]</a></span><br>
		
[$ endwhile $]
[$ endif $]
    </BODY>
</HTML>